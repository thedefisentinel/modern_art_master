"""强化学习:把 engine 包成自对弈环境,训练一个 3–5 人通用的策略。

设计要点(见 docs 或 README 的第三期):
  * 目标不是"最优解"(多人不完全信息博弈无独立于对手的最优),而是
    自对弈训练出的**强且低可利用性**策略,用胜率 + 可利用性评估。
  * 观测:自己手牌 + 公开面板 + 对手行为/收藏 + 拍卖上下文(让网络能推测对手与结局)。
  * 动作:参数化 —— 离散头选"拍哪张/出价与否",连续头出"多少钱"。
  * 奖励:相对分(终局现金 − 全场平均),零和化。
  * 算法:参数共享 PPO 自对弈 + 对手池 + 辅助信念头(预测对手手牌/本回合名次)。

模块:
  encoding.py  观测/动作编解码 + 合法掩码(引擎无关的纯函数,已单测)
  env.py       薄自对弈环境(reset/observe/step)
  train.py     PPO 训练(GPU)
"""
